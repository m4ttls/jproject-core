<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:task="http://www.springframework.org/schema/task"
	xmlns:tx="http://www.springframework.org/schema/tx"	
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/context 
	http://www.springframework.org/schema/context/spring-context-3.0.xsd	
	http://www.springframework.org/schema/task 
	http://www.springframework.org/schema/task/spring-task-3.0.xsd
   	http://www.springframework.org/schema/tx
   	http://www.springframework.org/schema/tx/spring-tx-3.0.xsd">

   	<context:annotation-config/>
	<context:component-scan base-package="ar.com.lakaut.sig.core" />

	<!-- Configuracion de Data Source -->
	<context:property-placeholder location="classpath:jdbc.properties"/>

	<bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" 	value="${hibernate.connection.driver.class}" />
		<property name="url" 				value="${hibernate.connection.url}" />
		<property name="username" 			value="${hibernate.connection.username}" />
		<property name="password" 			value="${hibernate.connection.password}" />
	</bean>

	<!-- Configuracion de Session Factory -->
	<bean id="sessionFactory"  class="org.springframework.orm.hibernate4.LocalSessionFactoryBean" >
		<property name="dataSource" ref="dataSource"/>
    	<property name="packagesToScan" value="ar.com.lakaut.sig.core.domain"/>
		<property name="hibernateProperties">
			<props>
				<prop key="hibernate.dialect">${hibernate.dialect}</prop>
				<prop key="hibernate.show_sql">${hibernate.show_sql}</prop>
				<prop key="hibernate.format_sql">${hibernate.format_sql}</prop>
			    <!-- configuration pool via c3p0 
			    <prop key="hibernate.connection.provider_class">${c3p0.hibernate.connection.provider_class}</prop>      
			    <prop key="hibernate.c3p0.acquire_increment">${c3p0.hibernate.c3p0.acquire_increment}</prop>
			    <prop key="hibernate.c3p0.idle_test_period">${c3p0.hibernate.c3p0.idle_test_period}</prop>
			    <prop key="hibernate.c3p0.max_size">${c3p0.hibernate.c3p0.max_size}</prop>
			    <prop key="hibernate.c3p0.max_statements">${c3p0.hibernate.c3p0.max_statements}</prop>
			    <prop key="hibernate.c3p0.min_size">${c3p0.hibernate.c3p0.min_size}</prop>
			    <prop key="hibernate.c3p0.timeout">${c3p0.hibernate.c3p0.timeout}</prop>
			    -->
			</props>
		</property>
	</bean>

	<!-- Defino transaction manager de hibernate -->
	<bean id="transactionManager" class="org.springframework.orm.hibernate4.HibernateTransactionManager">
		<property name="sessionFactory" ref="sessionFactory" />
	</bean>

	<!-- Defino Transacciones por ANNOTATION -->
	<tx:annotation-driven />


	 <!-- Email -->
	<bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
		<property name="host" value="192.168.206.22"/>
        <!-- <property name="port" value="587"/>
        <property name="username" value="webmaster"/>
        <property name="password" value="webmaster"/> -->
		<property name="javaMailProperties">
			<props>
				<prop key="mail.debug">true</prop> 
<!-- 				<prop key="mail.smtp.auth">true</prop>
				<prop key="mail.smtp.starttls.enable">true</prop> -->
			</props>
		</property> 
	</bean>
 
	<bean id="simpleMailMessage" class="ar.com.lakaut.sig.core.mail.SimpleEmailManager">
		<property name="mailSender" ref="mailSender" />
		<property name="simpleMailMessage" ref="customeMailMessage" />
	</bean>
    
	<bean id="customeMailMessage"
		class="org.springframework.mail.SimpleMailMessage">
	 
		<property name="from" value="webmaster@lakaut.com.ar" />
		<property name="subject" value="Nuevo Usuario" />
		<property name="text">
		   <value>
			<![CDATA[
				Hola %s,
				User: %s
				Password: %s
			]]>
		   </value>
	        </property>
	</bean>
</beans>
